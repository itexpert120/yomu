# Project-specific release shrinker rules.
#
# Keep this file intentionally small. AndroidX, Compose, Hilt, Room, Coil, and Readium
# publish consumer ProGuard rules through their dependencies; add rules here only when
# a release build or runtime smoke test proves that app-owned code needs preserving.
